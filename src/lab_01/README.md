# Лабораторная работа №1: Библиотека 2 вариант
## Цель работы
* Освоить объявление пользовательских классов.

* Разобраться с инкапсуляцией (*закрытые поля*).

* Реализовать свойства (*@property*).

* Переопределить магические методы (*__str__, __repr__, __eq__*).

* Понять разницу между атрибутами класса и экземпляра.

## Основные идеи
* **Инкапсуляция** – данные книги (название, автор, год издания, количество страниц, статус выдачи) скрыты за защищёнными атрибутами (с одним подчёркиванием), а доступ к ним организован через свойства **(@property)** с методами-валидаторами.

* **Проверка корректности данных** – при создании объекта и изменении его атрибутов выполняются проверки типов и логических ограничений (например, год в диапазоне 1600–2026, количество страниц > 0, название и автор не пустые). Валидация вынесена в отдельный модуль **validation.py**, что улучшает читаемость и повторное использование.

* **Атрибуты класса** – продемонстрировано использование атрибута, общего для всех экземпляров: счётчик созданных книг **total_books**.

* **Магические методы** – переопределены **__str__** (для пользовательского вывода),**__repr__** (для отладки) и **__eq__** (для сравнения объектов по названию, автору и году).

* **Бизнес-логика, зависящая от состояния** – методы **issue()** и **return_book()** реализуют поведение, которое меняется в зависимости от текущего статуса книги **(_is_loaned)**: нельзя выдать уже выданную книгу, нельзя вернуть доступную.

* **Обработка исключений** – при нарушении условий валидации или бизнес-правил выбрасываются исключения **TypeError**, **ValueError** или **RuntimeError**, которые перехватываются в демонстрационном скрипте, показывая корректную реакцию программы на недопустимые действия.

Таким образом, код наглядно иллюстрирует, как можно создавать надёжные, самопроверяемые классы с понятным интерфейсом и внутренней логикой.

## Реализованный класс Book
Представляет книгу в библиотеке с названием, автором, годом издания, количеством страниц и состоянием (доступна / выдана).

## Атрибуты класса
* **total_books** – целое число, счётчик созданных экземпляров класса. Увеличивается при каждом вызове конструктора.

## Атрибуты экземпляра (защищённые)
* **_title** – строка, название книги.

* **_author** – строка, автор книги.

* **_year** – целое число, год издания.

* **_pages** – целое положительное число, количество страниц.

* **_is_loaned** – булево значение, *True* – книга выдана, *False* – доступна.

## Методы валидации (вынесены в отдельный модуль validation.py)
* **validate_title(value)** – проверяет, что значение является непустой строкой.

* **validate_author(value)** – проверяет, что значение является непустой строкой.

* **validate_year(value)** – проверяет, что значение – целое число в диапазоне 1600-2026.

* **validate_pages(value)** – проверяет, что значение – целое положительное число.

## Свойства (@property)
* **title** – геттер и сеттер с вызовом *validate_title*.

* **author** – геттер и сеттер с вызовом *validate_author*.

* **year** – геттер и сеттер с вызовом *validate_year*.

* **pages** – геттер и сеттер с вызовом *validate_pages*.

* **is_loaned** – геттер (только чтение) для статуса выдачи.
  
  * **Геттер** (от англ. get — получать) — это метод, который возвращает значение закрытого атрибута.
  * **Сеттер** (от англ. set — устанавливать) — это метод, который изменяет значение закрытого атрибута, обычно с предварительной проверкой (валидацией).

## Бизнес-методы
* **issue()** – выдать книгу. Доступна только при статусе _is_loaned == False. Если книга уже выдана, генерирует исключение RuntimeError.

* **return_book()** – вернуть книгу. Доступна только при статусе _is_loaned == True. Если книга не была выдана, генерирует исключение RuntimeError.

## Магические методы
* **__str__()** – возвращает строку вида: «Война и мир» Лев Толстой (1869) – доступна (или «выдана»).

* **__repr__()** – возвращает строку, которая могла бы быть использована для воссоздания объекта: Book(title='Война и мир', author='Лев Толстой', year=1869, pages=1300, is_loaned=False).

* **__eq__(other)** – сравнивает текущий объект с other по названию, автору и году. Если other не является экземпляром Book, возвращает False.

## Описание сценариев демонстрации (по скриншотам)
### Сценарий 1: Создание объектов и магические методы __str__ и __repr__
<img width="1337" height="251" alt="image" src="https://github.com/user-attachments/assets/71d53fae-24de-4f1e-b923-4da6f0fa7bb1" />

* Создаются две книги: **book1*8 («Война и мир», Лев Толстой, 1869, 1300 стр.) и **book2** («Преступление и наказание», Фёдор Достоевский, 1866, 400 стр.).

* Вывод через **print(book1)** и **print(book2)** демонстрирует работу метода __str__: отображается название, автор, год и статус (доступна).

* Вызов **repr(book1)** и **repr(book2)** показывает официальное представление объектов, которое можно использовать для их воссоздания.
 ## Сценарий 2: Атрибут класса total_books
 <img width="438" height="98" alt="image" src="https://github.com/user-attachments/assets/afb5ee55-3029-4506-a5cb-da9a51991fca" />

 *После создания двух книг атрибут класса **Book.total_books** равен 2.

* Доступ к атрибуту класса возможен как через сам класс **(Book.total_books)**, так и через экземпляр **(book1.total_books)**, что подтверждает общий характер этого счётчика.

## Сценарий 3: Сравнение книг через __eq__
<img width="327" height="100" alt="image" src="https://github.com/user-attachments/assets/8128a7da-230d-42c7-8192-a009b4d87c80" />

* Создаётся **book3** с теми же параметрами, что и **book1**.

* Сравнение **book1 == book2** возвращает *False*, так как книги разные.

* Сравнение **book1 == book3** возвращает *True*, так как совпадают название, автор и год (критерий равенства, определённый в __eq__).

## Сценарий 4: Бизнес-методы с изменением состояния (issue / return_book)
<img width="839" height="195" alt="image" src="https://github.com/user-attachments/assets/e80f46a5-3731-4f98-990f-c8ac4e77b4af" />

* Исходно **book1** доступна *(is_loaned = False)*.

* Вызов **issue()** переводит книгу в состояние *«выдана»* – статус меняется.

* Повторная попытка выдачи вызывает исключение **RuntimeError** с сообщением «Книга 'Война и мир' уже выдана».

* Вызов **return_book()** возвращает книгу в доступное состояние.

* Повторный возврат приводит к исключению «Книга 'Война и мир' не была выдана».

* Сценарий наглядно показывает, как поведение методов зависит от внутреннего состояния объекта.

## Сценарий 5: Свойства (сеттеры) с валидацией
<img width="909" height="213" alt="image" src="https://github.com/user-attachments/assets/9735b109-ab0f-4dd6-955c-0751ca984918" />

* Для **book2** выводится текущий год (1866).

* Корректное изменение года на 1867 проходит успешно.

* Попытка установить год 3000 (вне допустимого диапазона 1600–2026) перехватывается исключением **ValueError**.

* Аналогично с количеством страниц: изменение на 450 успешно, попытка установить отрицательное значение (-100) вызывает ошибку.

* Демонстрируется работа сеттеров year и pages с вызовом валидации из модуля **validation.py**.

## Сценарий 6: Валидация при создании объектов (конструктор)
<img width="768" height="162" alt="image" src="https://github.com/user-attachments/assets/472ed826-d285-4ec1-a652-a15573ba2432" />

* Четыре попытки создать объект **Book** с некорректными параметрами:

  * *Пустое название* – ошибка «Название не может быть пустым».
  * *Пустой автор* – ошибка «Автор не может быть пустым».
  * *Год 1300 (вне диапазона)* – ошибка «Год должен быть в диапазоне 1600–2026».
  * *Отрицательное количество страниц* – ошибка «Количество страниц должно быть положительным».
* Каждая попытка обрамлена в **try/except**, что позволяет программе продолжить работу и показать все возможные ошибки валидации.





